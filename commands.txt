# Инструкция требует доработки, пользователь eskvisor не интегрирован в продукт
#____________________________________Установка библиотек для работы гипервизора_________________________________
# Для Ubuntu/Debian:
sudo apt update
sudo apt install -y qemu-kvm libvirt-daemon-system libvirt-clients bridge-utils virtinst
sudo apt install lvm2
sudo apt install numactl
sudo apt install nfs-kernel-server
sudo apt install nfs-common
sudo apt install virt-viewer
sudo apt install ovmf
sudo apt install libguestfs-tools -y

# Для CentOS/RHEL/ALT Linux:
sudo yum install -y qemu-kvm libvirt libvirt-client virt-install
sudo yum install lvm2
sudo yum install numactl
sudo yum install nfs-kernel-server
sudo yum install nfs-common
sudo yum install virt-viewer
sudo yum install edk2-ovmf
sudo yum install libguestfs-tools -y

# Для Fedora:
sudo dnf install -y qemu-kvm libvirt libvirt-client virt-install
sudo dnf install lvm2
sudo dnf install numactl
sudo dnf install nfs-kernel-server
sudo dnf install nfs-common
sudo dnf install virt-viewer
sudo dnf install edk2-ovmf
sudo dnf install guestfs-tools -y

sudo systemctl start libvirtd
sudo systemctl enable nfs-server
sudo systemctl start nfs-server
sudo systemctl enable libvirtd

sudo mkdir /eskvisor/storages/
sudo chmod 777 /eskvisor/storages/

# Проверьте статус (должен быть "active (running)")
sudo systemctl status libvirtd

# команда ниже нужна для корректной работы nfs хранилищ для тестов
sudo sh -c "echo '/srv/nfs 127.0.0.1(rw,sync,no_root_squash,fsid=0,no_subtree_check)' >> /etc/exports"
sudo exportfs -ra
#_________________________________Для подключаемых NFS хранилищ______________________________
sudo chmod -R 777 /mnt/ha_cluster/libvirt/
udo chmod -R 777 /mnt/ha_cluster/libvirt/qemu/
# sudo /usr/bin/python3 -m pytest --tb=auto /home/eska/eskvisor/agent/client/tests/virtual_machine - запуск тестов
# Окружение python нужно устанавливать как обычное так и под sudo

#_________________________________Создание пользователя eskvisor__________________________________________________
# Создать пользователя
sudo adduser eskvisor
sudo EDITOR=nano visudo
#_________________________________Настройка путей______________________________
sudo EDITOR=nano visudo
ALL ALL=(ALL) NOPASSWD: /bin/mkdir -p /sys/fs/cgroup/libvirt/*
ALL ALL=(ALL) NOPASSWD: /usr/bin/tee /sys/fs/cgroup/libvirt/*
ALL ALL=(ALL) NOPASSWD: /bin/echo * > /sys/fs/cgroup/libvirt/*
ALL ALL=(ALL) NOPASSWD: /bin/cat /sys/fs/cgroup/libvirt/*
ALL ALL=(ALL) NOPASSWD: /usr/bin/bash -c echo\ * >\ /sys/fs/cgroup/libvirt/*
ALL ALL=(ALL) NOPASSWD: /bin/rmdir /sys/fs/cgroup/libvirt/*

sudo systemctl daemon-reload
sudo systemctl enable setup-cgroup-libvirt.service
sudo systemctl start setup-cgroup-libvirt.service
# Дать права sudo (требует ввода пароля)
sudo usermod -aG sudo eskvisor  # Для Ubuntu/Debian
# ИЛИ
sudo usermod -aG wheel eskvisor  # Для CentOS/RHEL
sudo usermod -a -G cdrom,libvirt,kvm,input,disk,adm,cgroup eskvisor
sudo usermod -a -G libvirt eskvisor
sudo usermod -a -G kvm eskvisor
sudo usermod -aG qemu eskvisor
sudo usermod -aG disk eskvisor
sudo passwd eskvisor

newgrp libvirt
newgrp kvm
newgrp qemu
newgrp disk

# Указываем пароль: P@$$w0rd12A

sudo systemctl daemon-reload
sudo systemctl restart virtqemud.socket
sudo systemctl restart virtqemud

# Нагрузить ОЗУ на 1 ГБ
# timeout 5 stress --vm 1 --vm-bytes 1G --vm-keep

# Нагрузить все ядра на 5 секунд
# timeout 5 stress --cpu $(nproc) --timeout 5s