#!/bin/bash
# /etc/systemd/system/cgroup-state-timer.timer
[Unit]
Description=Periodic CGroup Backup Timer
Requires=cgroup-state.service

[Timer]
# Сохраняем каждые 6 часов
OnCalendar=*-*-* 0/6:00:00
Persistent=true
RandomizedDelaySec=300

Unit=cgroup-state.service
[Install]
WantedBy=timers.target
